{
  "name": "careers-page-builder",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev:client\"",
    "dev:client": "wait-on http://localhost:3000/api/health && npm run dev --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^9.0.1"
  }
}
